server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/unwind
    username: unwind
    password: unwind
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: saga
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin